# Test requirements for the Ajax Security integration.
#
# pytest-homeassistant-custom-component pins the ENTIRE Home Assistant test
# toolchain (pytest, pytest-asyncio, pytest-cov, coverage, homeassistant,
# aiohttp, ...) to the exact versions the targeted HA release uses. Do NOT add
# version floors for any of those here: they fight HA's exact pins and make the
# install unresolvable (that is what kept the CI tests job disabled). Only
# declare what phcc does not already pull in.
pytest-homeassistant-custom-component>=0.13.345

# Not provided by phcc:
pytest-mock

# Integration runtime dep needed to import the package under test (kept in sync
# with manifest.json).
aiobotocore>=3.7.0
