# État overhaul — refactor/integration-overhaul-2026-07-07

> Fichier d'état durable du skill ha-integration-overhaul. Supprimé au commit final (Phase 9).

## Phase courante
Phase 6 — Config flow & translations (strings monitored_spaces, parité 7 langues)

## Phases terminées
- ✓ Phase 0 — Préflight (pas de commit — branche + tag seulement)
  - Branche : `refactor/integration-overhaul-2026-07-07`
  - Tag backup : `backup/before-overhaul-20260707-065700`
  - Base : main @ a2c9b8ea (v0.34.3 + 2 bumps Renovate)
  - HA dev : container `homeassistant` (HA 2026.7.0), copie déployée = v0.34.2 (code identique, seul manifest diffère)
- ✓ Phase 0.5 — État de l'art web
  - Document : `/tmp/claude-1000/-mnt-39c0f0e6-4018-4aa1-8d96-24720083fa77-Codage-GitHub-Ajax/9f620c73-0a3e-42b6-8ad3-533467affdc5/scratchpad/ha-backend-state-of-art-2026-07-07.md`
  - Points critiques :
    1. **Dépréciation config entry update listener + reload methods** (2026.6 → erreur 2026.12) — vérifier `add_update_listener` dans __init__.py/config_flow*.py ; remplacement : `async_update_and_abort()` / `reload_on_update=False` / OptionsFlow auto-reload. Réf : blog dev 2026/05/07.
    2. Dépréciation `show_advanced_options` (retrait 2027.6) — remplacer par sections UI si utilisé.
    3. Dépréciation entity_id à domaine incompatible (retrait 2027.5) — ne jamais fixer entity_id manuellement.
    4. Best practices coordinator inchangées (`always_update=False`, `async_config_entry_first_refresh`, CoordinatorEntity).
    5. Quality Scale : Bronze 20 / Silver 10 / Gold ~21-24 / Platinum 3 (async-dependency, inject-websession, strict-typing).

## Baseline (Phase 1.5 / 1.5.bis)
- **unique_id avant** : 255 entités, sauvegardées dans scratchpad `uids-before-20260707.txt` ET copie repo `tests/fixtures/` non — voir scratchpad. Extraction : entity_registry HA dev, platform=ajax.
- **Perf avant** : `Setup of domain ajax took 0.00 seconds` (boots 2026-07-07 06:32:36 et 06:49:05, HA 2026.7.0). 0 erreur / 0 warning ajax depuis boot. importtime dominé par HA core (litellm via hass_nabucasa) ; coût propre ajax ≈ 0,7 ms.
- Ruff (lint+format) : 100 % propre avant refactor. 63 fichiers de tests existants.

## Copie de l'inventaire unique_id (255 entrées)
→ scratchpad `uids-before-20260707.txt` (régénérable : `docker exec homeassistant python3` sur `/config/.storage/core.entity_registry`, platform == "ajax")

- ✓ Phase 1 — Découverte (rapport agent vérifié sur disque, résumé présenté)
  - Bugs confirmés : (1) SSE `_find_device` sans prefix-match vs SQS ; (2) options AWS sans reload ;
    (3) options RTSP sans reload (ONVIF bootstrap-only) ; (4) CONF_MONITORED_SPACES jamais lu ;
    (5) THERMOSTAT/TEMPERATURE_SENSOR/LINE_SPLITTER sans handler → skip silencieux.
  - Dette : langues de/nl/sv/uk mortes dans event_codes (SUPPORTED_LANGUAGES limité) ;
    get_alarm_control_panels() jamais appelé ; ACTION_CATEGORIES calculé jamais exposé ;
    duplication _build_X ×10 plateformes ; 6 blocs optimistic-update dans alarm_control_panel ;
    tags événements en dur ; PARALLEL_UPDATES=1 sur plateformes read-only (reco QS = 0) ;
    commentaire quality_scale.yaml parallel-updates contredit le code.
  - Issue #167 : DÉJÀ corrigée dans le code (_device_type_map.py:183-186).
  - Baseline tests : 1918 passed, 99 % (9860 stmts / 124 miss).
- ✓ Phase 2 — Architecture (commit : voir git log "refactor(options)")
  - Filtre monitored_spaces implémenté (_coordinator_events + sqs_manager, space_id param).
  - Reload sur changement AWS creds + RTSP creds dans l'options flow (si changement réel).
  - reauth_confirm + 2FA reauth → helper moderne async_update_reload_and_abort.
  - Tests : 1925 passed (7 nouveaux), déploiement HA dev OK (0 erreur, ONVIF 2/2).
- ✓ Phase 3 — Refactor structurel (commit : voir git log "refactor: dead code...")
  - Supprimé get_alarm_control_panels (base+hub+2 tests) — jamais appelé en prod.
  - SUPPORTED_LANGUAGES étendu à 7 langues (réactive ~550 trads de/nl/sv/uk mortes) ;
    set_language SQS ne clampe plus sur (fr,en,es) ; connecteur « par » ×7 langues.
  - GROUP_ARM_EVENT_TAGS / FULL_ARM_EVENT_TAGS dans event_maps (remplace littéraux ×5).
  - _async_optimistic_transition factorise les 5 blocs optimistic/rollback d'alarm_control_panel (−120 l.).
  - Setup statique replié sur les builders de découverte : event, camera, switch, light, valve,
    update, device_tracker (−250 l. de duplication ; descriptors en un seul endroit).
  - button/alarm_control_panel : duplication d'une ligne, non repliés (décision).
  - _coordinator_devices.py (776 l., 84 ifs de parsing) : NON splitté cette passe — chemin chaud,
    100 % couvert, bien commenté ; noté dette restante.
  - Mocks tests corrigés (get_space réel), 1924 passed, déploiement HA dev OK (0 erreur).
- ✓ Phase 4 — Client API/push (commit : "fix(sse): prefix-match…")
  - SSE _find_device : ajout stratégie prefix (parité SQS, WireInput/MultiTransmitter en mode proxy).
  - Vérifié : timeouts/backoff/refresh 401 déjà conformes ; zéro credential loggé.
  - 1925 passed, déploiement HA dev OK (0 erreur/warning).
- ✓ Phase 5 — Entités (commit : "feat(devices): generic handler…")
  - devices/generic.py : GenericHandler (batterie/tamper/signal/firmware/problème + température si présente)
    mappé sur THERMOSTAT, TEMPERATURE_SENSOR, LINE_SPLITTER — plus aucun type reconnu sans handler
    (restants SMART_LOCK/CAMERA/VIDEO_EDGE/UNKNOWN = volontairement hors table).
  - PARALLEL_UPDATES = 0 sur sensor/binary_sensor/device_tracker (reco QS read-only) + quality_scale.yaml corrigé.
  - Attribut `category` exposé sur le sensor last event (ACTION_CATEGORIES enfin consommé).
  - 1928 passed, déploiement HA dev OK (0 erreur).

## Décisions utilisateur
- Aucune validation bloquante demandée (règles d'autonomie CLAUDE.md) ; résumé Phase 1.6 présenté dans la conversation.
- unique_id : format actuel conservé (shippé, stable, préfixé entry_id) — AUCUNE migration.

## TODO restants
- Phase 3 : code mort (get_alarm_control_panels, langues mortes→réactivation 7 langues,
  constantes tags), factorisation optimistic-transition + _build_X, split éventuel _coordinator_devices
- Phase 4 : SSE prefix-match
- Phase 5 : handlers manquants (warning + éventuel handler générique), PARALLEL_UPDATES read-only → 0,
  category dans attributs last event
- Phase 6 : strings des options si besoin, parité 7 langues
- Phase 7 : quality_scale.yaml commentaire parallel-updates, mypy/hassfest
- Phase 8-9 : tests + validation finale
